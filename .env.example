# Vercel Postgres (se configura automáticamente al vincular en Vercel)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Vercel Blob (se configura automáticamente al vincular en Vercel)
BLOB_READ_WRITE_TOKEN=

# Pollinations.ai API key (obtener en https://enter.pollinations.ai)
POLLINATIONS_API_KEY=sk_tu-api-key-de-pollinations

# Cron job secret (para verificar llamadas de Vercel Cron)
CRON_SECRET=tu-string-secreto-random
